# Project Plan: 전공어 산성비 게임 (TDD & Local NLP Version)

이 문서는 테스트 주도 개발(TDD) 방식을 기반으로 작성된 작업 지시서입니다. AI 어시스턴트는 반드시 **[Test 작성] -> [로직 구현] -> [Test 통과 확인]**의 사이클을 엄격하게 준수하며, 한 단계가 완료될 때마다 체크박스(`[x]`)를 업데이트해야 합니다.

---

## Phase 1: 기본 레이아웃 및 디버그 환경 구축
실제 마이크 없이도 로직을 시각적으로 테스트할 수 있는 뼈대를 만듭니다.

- [x] **UI 구조 렌더링:** `index.html`, `style.css` 작성. (다크 모드, 네온 그린 테마)
- [x] **[Layout]** 타자 입력창(`<input>`)을 화면 **최하단(Bottom: 0)**에 가로로 길게 고정 배치.
- [x] **[Layout]** 우측 상단 '자막 패널', 중앙 '게임 캔버스' 영역 확보.
- [x] **[Debug UI]** 좌측 상단 구석에 '텍스트 주입 입력창'과 '주입(Inject)' 버튼 생성. (도서관 테스트용)
- [x] **시각적 검증:** 주입 버튼 클릭 시 입력한 텍스트가 콘솔에 정상 출력되는지 확인.

## Phase 2: Local NLP 엔진 구현 (TDD 핵심 구간)
단어가 화면에 떨어지기 전, 필터링 로직의 정확성을 `console.assert`로 먼저 검증합니다.

- [x] **[Test 작성]** `script.js`에 `testNLPEngine()` 함수를 만들고 아래 케이스를 검증하는 코드 작성.
  * *입력:* "오늘 배울 메모리가 아주 중요합니다." -> *기대:* `['메모리', '아주', '중요']`
  * *입력:* "자, 포인터는 어렵습니다." -> *기대:* `['포인터', '어렵']`
- [x] **[로직 구현]** `processText(text)` 함수 구현. (토큰화 -> 특수문자 제거 -> 조사/어미 제거 정규식 -> 1글자 제거 -> 불용어 필터링)
- [x] **[검증]** 브라우저 콘솔에서 모든 테스트 케이스가 에러 없이 통과(Pass)하는지 확인.

## Phase 3: 타자 게임 물리 엔진 및 매칭 로직 (Edge Case 반영)
추출된 단어가 떨어지고, 사용자가 이를 파괴하는 핵심 물리 로직을 구현합니다.

- [x] **[로직 구현: 낙하]** 큐에 들어온 단어들이 화면 상단 무작위 위치에서 생성되어 아래로 떨어지는 애니메이션 구현 (`requestAnimationFrame`).
- [x] **[로직 구현: 매칭]** 하단 입력창 엔터 시 매칭 로직 구현.
  * **Rule 1:** 화면에 없는 단어 입력 시 입력창만 즉시 비우고 무시.
  * **Rule 2:** 동일한 단어가 여러 개일 경우, **가장 아래쪽(Y좌표가 가장 큰)** 단어 하나만 제거.
- [x] **[로직 구현: 밸런스]** 화면에 동시에 존재하는 단어가 10개를 초과할 경우 새로운 단어 스폰을 일시 중단하는 로직 추가.
- [x] **[검증]** 디버그 패널로 단어를 대량 주입하여 낙하 속도 및 매칭 룰(가장 낮은 단어 우선 파괴)이 정상 작동하는지 확인.

## Phase 4: Web Speech API (STT) 통합
로직 검증이 완료된 파이프라인에 실제 마이크 음성을 연결합니다.

- [x] **[로직 구현]** `webkitSpeechRecognition` 설정 및 마이크 권한 요청.
- [x] **[로직 구현: 자막]** 실시간 인식 결과(`interimResults`)를 우측 자막 패널에 즉각 표시.
- [x] **[파이프라인 연결]** 최종 인식 문장(`isFinal`) 발생 시 `processText()`를 거쳐 게임 큐로 단어 전달.
- [x] **[방어 로직]** `onend` 이벤트에서 마이크 자동 재시작 로직을 추가하여 무한 수신 상태 유지.

## Phase 5: 데모 최적화 및 클린업 (발표 준비)
학회 시연을 위해 개발용 UI를 정리하고 시각적 완성도를 높입니다.

- [x] **[Cleanup]** 모든 기능 검증 완료 후, 좌측 상단의 **'디버그 테스트 패널'을 화면에서 숨김** 처리 (`display: none`).
- [x] **[UI Polish]** 단어 파괴 시 작은 파티클 효과 또는 점수 상승 애니메이션 추가.
- [x] **[최종 테스트]** 마이크에 대고 전공 용어가 포함된 문장을 말했을 때, 조사 없이 핵심 키워드만 비처럼 떨어지는지 전체 워크플로우 최종 점검.

## Phase 6: 난이도 조절 및 목숨(Life) 시스템 추가
게임의 완성도를 높이기 위해 엣지 케이스를 처리하고 생존/난이도 로직을 도입합니다.

- [x] **[UI 업데이트]** 상단 점수 표시줄 옆에 '목숨(❤️)' UI를 추가하고, 평소에는 숨겨져 있을 '게임 오버 모달창(최종 점수, 다시 시작 버튼 포함)'을 HTML/CSS로 미리 작성.
- [x] **[로직 구현: 목숨 차감]** 낙하 애니메이션 루프 내에서, 단어의 Y좌표가 컨테이너의 바닥 높이를 초과했는지 체크하는 조건문 추가. 바닥에 닿으면 큐에서 제거하고 `life` 변수를 1 감소시키며 UI(❤️) 업데이트.
- [x] **[로직 구현: 속도 증가]** 낙하 속도 변수에 점수(`score`)를 곱하거나 더하여, 점수가 오를수록 단어가 밑으로 더 빠르게 이동하도록 속도 공식 수정.
- [x] **[로직 구현: 게임 오버]** `life`가 0이 되면 동작을 멈추는 `gameOver()` 함수 구현. 애니메이션 캔슬(`cancelAnimationFrame`), STT 중단(`recognition.stop()`), 게임 오버 모달창 표시(`display: block`) 처리.
- [x] **[로직 구현: 리셋]** 모달창의 '다시 시작' 버튼 클릭 시, 점수/목숨/낙하 큐 등 모든 상태를 초기화하고 게임을 처음부터 다시 시작하는 `resetGame()` 연동.
- [x] **[최종 TDD 검증]** 디버그 패널로 단어를 대량 주입한 뒤 타자를 치지 않고 기다려서 바닥에 닿을 때마다 하트가 깎이고 0이 되면 게임이 멈추는지(게임 오버) 확인. 다시 시작 버튼이 정상 작동하는지 점검.

## Phase 7: 스폰 딜레이(Spawn Cooldown) 및 초기 속도 밸런싱
단어가 한꺼번에 쏟아지는 폭주 현상을 막고, 쾌적한 플레이 경험을 위한 밸런스를 잡습니다.

- [x] **[로직 수정: 순차 스폰]** STT로 인식된 단어 배열이 들어오면 화면에 즉시 그리지 않고 `wordQueue` 배열에만 저장.
- [x] **[로직 수정: 타이머]** `spawnTimer` 또는 `lastSpawnTime` 변수를 도입하여, 최소 800ms ~ 1000ms의 쿨타임이 지났을 때만 `wordQueue`에서 단어를 하나 꺼내(`shift()`) 화면 상단에 생성하도록 스폰 로직 변경.
- [x] **[로직 수정: 속도 조절]** 초기 낙하 속도(baseSpeed)를 대폭 하향(예: 0.5 ~ 1.0)하여 천천히 떨어지게 조정.
- [x] **[검증]** 디버그 패널에 긴 문장을 주입했을 때, 단어들이 한 번에 겹쳐서 떨어지지 않고 약 1초 간격으로 예쁘게 줄지어 내려오는지, 그리고 떨어지는 속도가 적당히 느린지 확인.