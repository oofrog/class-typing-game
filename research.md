# Research Notes: 전공어 산성비 게임 (Local NLP Version)

이 문서는 AI 코딩 어시스턴트가 코드를 작성할 때 반드시 준수해야 하는 기술 스택, 아키텍처 및 자체 필터링 알고리즘을 정의합니다. 외부 LLM API 통신을 완전히 배제하고, 브라우저 내부 자원만으로 실시간 처리하는 것이 핵심입니다.

## 1. 프로젝트 개요 및 아키텍처 (Overview & Architecture)
* **목표:** 발표자의 음성을 실시간으로 인식(STT)하고, 브라우저 단에서 즉각적인 텍스트 전처리(NLP)를 거쳐 핵심 단어만 타자 게임으로 떨어뜨리는 웹 어플리케이션.
* **아키텍처 특징:** Zero-Backend, No-API. 모든 로직은 클라이언트(브라우저)에서 동기/비동기적으로 처리됨.
* **배포 환경:** GitHub Pages (`github.io`) 기반 순수 정적 호스팅.

## 2. 기술 스택 및 제약 사항 (Tech Stack & Constraints)
* **프론트엔드:** HTML5, CSS3, Vanilla JavaScript (ES6+).
* **[CRITICAL] 금지 사항:** * OpenAI, Gemini 등 외부 API 호출(`fetch`) 및 통신 로직 **절대 사용 금지**.
  * React, Vue 등 무거운 프레임워크 사용 금지.
  * API Key를 묻는 로그인 UI나 상태 관리 로직 제거.

## 3. UI/UX 및 디자인 규칙 (Design Guidelines)
* **전체 테마:** 해커톤/터미널 느낌의 다크 모드 (검은 배경, 네온 그린 텍스트 권장).
* **화면 구성:**
  * **게임 캔버스 영역:** 화면의 대부분(상단~하단 입력창 위까지)을 차지하는 영역. 위에서 아래로 단어가 떨어지는 공간.
  * **자막 패널:** 화면 우측에 위치. 실시간으로 인식되는 STT 문장을 끊김 없이 보여주는 영역.
  * **[CRITICAL] 타자 입력창 레이아웃:** 화면 **최하단(Bottom: 0)에 고정(Fixed/Absolute)** 배치. 화면 너비를 꽉 채우거나 가로로 넓게 뻗은 형태의 `<input>` 필드로 구성하여 게임 중 항상 하단에 위치하도록 CSS를 작성할 것. (입력 후 엔터 시 매칭 단어 삭제 및 점수 획득)
  * **테스트 패널 (디버깅용):** 화면 좌측 상단 구석. 마이크 사용이 불가한 환경(도서관 등)을 위해, 텍스트를 수동으로 입력하고 '수동 주입' 버튼을 누를 수 있는 조그만 UI.

## 4. 핵심 로직 A: Web Speech API (STT 엔진)
외부 서버 없이 브라우저 내장 음성 인식을 활용합니다.
* **사용 객체:** `webkitSpeechRecognition`
* **필수 옵션 설정:**
  * `continuous: true` (말을 쉬어도 마이크가 꺼지지 않음)
  * `interimResults: true` (중간 인식 결과도 자막 패널에 즉각 렌더링)
* **[CRITICAL] 방어 로직 (마이크 꺼짐 방지):** 브라우저 정책상 화자의 침묵이 길어지면 강제로 인식기가 종료될 수 있습니다. `recognition.onend` 이벤트 리스너에서 명시적인 종료 명령이 없었다면 `recognition.start()`를 재호출하여 마이크 수신을 무한 루프 상태로 유지할 것.

## 5. 핵심 로직 B: Local NLP 필터링 엔진 (단어 추출 알고리즘)
STT의 `onresult` 이벤트에서 최종 확정된 문장(`isFinal === true`) 또는 디버깅 입력창으로 들어온 텍스트를 즉시 가공하여 게임 단어로 만듭니다.
* **[CRITICAL] 단어 가공 파이프라인 (순서대로 적용할 것):**
  1. **토큰화:** 수신된 문장을 띄어쓰기(` `) 기준으로 분리하여 배열화.
  2. **특수기호 제거:** 정규식을 사용해 단어에 붙은 구두점(.,?! 등)을 제거.
  3. **조사 및 어미 제거 (정규식):** 단어 끝부분이 한국어 조사나 어미로 끝날 경우 잘라냄. (예: `/(은|는|이|가|을|를|에|에서|의|로|으로|입니다|합니다|습니다)$/g`) -> "메모리가"를 "메모리"로 변환.
  4. **길이 필터링:** 가공 후 단어의 길이가 1글자 이하인 경우 배열에서 탈락시킴 (예: '자', '음', '그').
  5. **불용어(Stop-word) 필터링:** 강의 중 흔히 나오는 의미 없는 단어 배열(예: `['오늘', '배울', '내용은', '여러분', '일단', '그리고', '그래서', '저번', '시간에']`)을 미리 하드코딩해 두고, 여기에 포함된 단어는 탈락시킴.
* **큐(Queue) 삽입:** 위 파이프라인을 통과하여 살아남은 단어들만 즉시 게임의 '단어 낙하 배열(Queue)'에 push 하여 화면 꼭대기에서 떨어지기 시작하도록 구현. 딜레이나 `setInterval` 없이 즉시 처리할 것.
[추가 권장 사항] 단어 스폰 제한: 게임 화면(Queue)에 동시에 떨어지고 있는 단어의 개수가 10개(또는 적정 개수)를 초과할 경우, 새로 들어온 단어는 화면에 추가하지 않고 무시하여 게임 밸런스를 유지할 것.
* **큐(Queue) 삽입 및 순차 스폰 (Staggered Spawn):** 필터링을 통과한 단어들은 즉시 화면에 생성되는 것이 아니라 '낙하 대기열(Queue) 배열'에만 보관됨.
* **[CRITICAL] 스폰 딜레이 로직:** `requestAnimationFrame` 루프 또는 `setInterval`을 사용하여, Queue에 단어가 있다면 **최소 0.8초 ~ 1초 이상의 간격(Cooldown)을 두고 한 번에 하나씩만 화면 꼭대기에서 생성(Spawn)**시킬 것. 절대 여러 단어가 동시에 같은 Y좌표에서 겹쳐서 떨어지면 안 됨.



## 6. 게임 난이도 및 생존 시스템 (Difficulty & Life System)
게임의 긴장감을 부여하기 위해 동적 속도 조절과 목숨 기반의 게임 오버 로직을 적용합니다.
* **[CRITICAL] 목숨(Life) 시스템:**
  * 초기 목숨은 5개(또는 3개)로 시작하며, UI 상단 점수 옆에 직관적인 아이콘(예: ❤️❤️❤️❤️❤️)으로 표시.
  * 떨어지는 단어를 타이핑하지 못하고 화면 맨 바닥(Bottom 경계선)에 닿을 경우, 해당 단어는 배열에서 즉시 삭제되고 목숨이 1개 차감됨.
  * 목숨이 0 이하가 되면 진행 중인 모든 애니메이션(`requestAnimationFrame`)과 STT 음성 인식을 즉각 중단하고 게임 오버 처리할 것.
* **동적 난이도 (Dynamic Speed):**
  * 현재 점수(Score)가 높아질수록 단어의 낙하 속도(Y축 이동량)가 정비례 또는 계단식으로 빨라지도록 수학적 공식을 적용할 것.
  * 공식 예시: `현재 떨어지는 속도 = 기본 속도 + (현재 점수 * 가중치)`
* **게임 오버 UI:** 게임 종료 시 화면 중앙에 반투명한 검은색 모달(Modal) 창을 띄우고, "Game Over" 문구와 함께 [최종 점수] 및 [다시 시작] 버튼을 표시.
* **초기 낙하 속도 하향:** 게임 시작 직후의 기본 낙하 속도(baseSpeed)를 매우 느리게(예: 프레임당 0.5px ~ 1px 이동) 설정하여 사용자가 적응할 시간을 줄 것.